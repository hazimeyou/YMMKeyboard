# v0.1.0a-rc1 Freeze Record

## Commit

- `ca58e73` `docs: prepare public readme and release draft`

## Tag

- `v0.1.0a-rc1`

## Build Result

- `dotnet build YMMKeyboardPlugin.slnx` succeeded
- Warnings: `0`
- Errors: `0`
- `cmake --build firmware/src/RP2040TinyUsb/build` succeeded
- Firmware side: `ninja: no work to do`

## Release Artifacts

- `YMMKeyboardPlugin.dll`
- `ymm_keyboard_fw.uf2`
- `README.md`
- `LICENSE`

## Known Limitations

- RP2040 firmware is required for the primary input path
- Legacy COM diagnostic fallback remains for observation and compatibility only
- No new macros, additional devices, advanced settings UI, or auto-update are included in `v0.1.0a`

## Release Readiness

The `v0.1.0a-rc1` state is fixed for release judgment.

Ready items:

- public README is prepared
- release draft is prepared
- release scope is frozen
- build is clean
- public artifacts are identified

Open items before final publication:

- GitHub Release draft creation
- final package assembly
- final tag promotion if needed
