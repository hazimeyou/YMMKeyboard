name: Macro Diagnostics Verification

on:
  push:
  pull_request:

jobs:
  verify_macro_diagnostics:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: "10.0.x"

      - name: Verify macro diagnostics foundation
        shell: pwsh
        run: ./scripts/verify-macro-diagnostics.ps1
